# Serverless Admin Dashboard

Full-stack admin dashboard application built with Next.js 15 and deployed on AWS AppRunner.

## 🚀 Features

- **Next.js 15** with App Router and React Server Components
- **React 19** with latest features
- **TypeScript** for type safety
- **Tailwind CSS v4** for styling
- **TailAdmin** UI components
- **AWS AppRunner** deployment with CDK
- **CloudWatch** monitoring and alarms
- **Docker** containerized deployment
- **CI/CD** with GitHub Actions

## 📁 Project Structure

```
/
├── frontend/          # Next.js application
├── backend/           # AWS Lambda functions
├── infra/             # AWS CDK infrastructure
└── docs/              # Documentation
```

## 🏃 Quick Start

### Local Development

```bash
# Install dependencies
cd frontend
npm install

# Run development server
npm run dev
```

Visit http://localhost:3000

### Deploy to AWS

See **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** for complete deployment instructions.

Quick deploy:

```bash
cd infra
npm install
npx cdk bootstrap
npm run deploy:dev
```

## 📚 Documentation

- **[DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)** - Complete AWS deployment guide
- **[infra/README.md](./infra/README.md)** - Infrastructure quick reference
- **[infra/docs/.kiro/](./infra/docs/.kiro/)** - Detailed infrastructure docs (generated by Kiro AI)
- **[docs/tech-stack.md](./docs/tech-stack.md)** - Technology stack details

## 🛠️ Tech Stack

### Frontend
- Next.js 15.x with App Router
- React 19
- TypeScript 5.x
- Tailwind CSS v4
- ApexCharts for data visualization
- FullCalendar for calendar features

### Infrastructure
- AWS AppRunner (container hosting)
- AWS CDK 2.x (Infrastructure as Code)
- AWS ECR (container registry)
- AWS CloudWatch (logging & monitoring)
- AWS Cognito (authentication - planned)

### Development Tools
- Biome (linting & formatting)
- Jest (unit testing)
- Playwright (E2E testing - planned)

## 💰 Cost

Estimated monthly cost with minimal traffic:
- **Development**: ~$3-5 USD/month
- **Production**: Scales with usage

## 🔧 Configuration

### Environment Variables

Edit `infra/config/app-config.ts`:

```typescript
environmentVariables: {
  DATABASE_URL: 'your-database-url',
  API_KEY: 'your-api-key',
}
```

### Custom Domain

```typescript
customDomain: 'admin.example.com'
```

### Instance Sizing

```typescript
instanceConfig: {
  cpu: '1 vCPU',
  memory: '2 GB',
}
```

## 📊 Monitoring

- CloudWatch alarms for CPU, memory, and errors
- Email notifications for critical issues
- Centralized logging in CloudWatch

## 🔄 CI/CD

GitHub Actions workflow automatically deploys:
- `develop` branch → dev environment (automatic)
- `main` branch → production environment (with approval)

**Setup Guide**: See [docs/.kiro/CI_CD_SETUP.md](./docs/.kiro/CI_CD_SETUP.md)

## 🧪 Testing

```bash
# Frontend tests
cd frontend
npm test

# Infrastructure tests
cd infra
npm test

# Test Docker build locally
cd frontend
docker build -t test .
docker run -p 3000:3000 test
```

## 📝 Available Scripts

### Frontend (`/frontend`)

```bash
npm run dev          # Start development server
npm run build        # Build for production
npm start            # Start production server
npm run lint         # Run linter
```

### Infrastructure (`/infra`)

```bash
npm run deploy:dev   # Deploy to development
npm run deploy:prod  # Deploy to production
npm run diff:dev     # Preview changes
npm run destroy:dev  # Delete dev stack
```

## 🏗️ Architecture

```
┌──────────────┐
│   GitHub     │
│  (CI/CD)     │
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐
│   AWS CDK    │────▶│     ECR      │
│  (Deploy)    │     │  (Images)    │
└──────────────┘     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  AppRunner   │
                     │  (Next.js)   │
                     └──────┬───────┘
                            │
                ┌───────────┼───────────┐
                ▼           ▼           ▼
         ┌──────────┐ ┌─────────┐ ┌─────────┐
         │CloudWatch│ │   SNS   │ │   IAM   │
         │   Logs   │ │ Alarms  │ │  Roles  │
         └──────────┘ └─────────┘ └─────────┘
```

## 🤝 Contributing

1. Create a feature branch
2. Make your changes
3. Test locally
4. Submit a pull request

## 📄 License

See [frontend/LICENSE](./frontend/LICENSE)

## 🆘 Support

- Check [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) for deployment issues
- Review CloudWatch logs for runtime errors
- See [docs/](./docs/) for additional documentation

## 🎯 Roadmap

- [ ] Cognito authentication integration
- [ ] RDS database setup
- [ ] Redis caching layer
- [ ] CloudFront CDN
- [ ] WAF security rules
- [ ] Automated backups
- [ ] Multi-region deployment

---

**Ready to deploy?** Check out the [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)! 🚀

---

## 📋 Using This Repository as a Template

When using this repository as a template for a new project, update the following files and values:

### 1. GitHub Repository Configuration

**File:** `infra/bin/app.ts`

```typescript
// Line 23: Update with your GitHub repository name
githubRepo: 'your-username/your-repo-name',
```

**File:** `.github/workflows/deploy.yml`

```yaml
# Line 110: Update the expected repository format in debug output (optional)
echo "Expected format: repo:your-username/your-repo-name:*"
```

### 2. AWS Stack Names

**File:** `infra/bin/app.ts`

```typescript
// Line 21: Update infrastructure stack name prefix
stackName: `your-project-infra-${environment}`,

// Line 29: Update application stack name prefix
stackName: `your-project-app-${environment}`,
```

**File:** `.github/workflows/deploy.yml`

```yaml
# Line 171: Update CloudFormation stack name to match your app stack name
--stack-name your-project-app-${{ steps.env.outputs.environment }} \
```

### 3. Application Configuration

**File:** `infra/config/app-config.ts`

```typescript
// Lines 36 & 54: Update email for CloudWatch alarms
alarmEmail: 'your-email@example.com',

// Lines 34 & 52: Add custom domain (optional)
customDomain: 'admin.yourdomain.com',

// Lines 41-45 & 59-61: Add environment variables
environmentVariables: {
  DATABASE_URL: 'your-database-url',
  API_KEY: 'your-api-key',
  // Add your environment variables here
},
```

### 4. Frontend Package Name (Optional)

**File:** `frontend/package.json`

```json
{
  "name": "your-project-name",
  "version": "1.0.0",
  // ... rest of the file
}
```

### 5. GitHub Secrets Setup

After deploying InfraStack, set these secrets in your GitHub repository:

**Settings → Secrets and variables → Actions**

1. **AWS_ROLE_ARN**: Copy from InfraStack deployment output
2. **AWS_REGION**: Your AWS region (e.g., `us-east-1`, `ap-northeast-1`)

### Quick Setup Checklist

- [ ] Update GitHub repository name in `infra/bin/app.ts`
- [ ] Update stack names in `infra/bin/app.ts` (2 places)
- [ ] Update stack name in `.github/workflows/deploy.yml`
- [ ] Update email in `infra/config/app-config.ts` (2 places: dev & prod)
- [ ] Add environment variables in `infra/config/app-config.ts` (if needed)
- [ ] Update frontend package name in `frontend/package.json` (optional)
- [ ] Deploy InfraStack: `cd infra && npx cdk deploy InfraStack-dev -c environment=dev`
- [ ] Set GitHub secrets: `AWS_ROLE_ARN` and `AWS_REGION`
- [ ] Deploy AppStack: `cd infra && npm run deploy:dev`

### Important Notes

- **Stack names** must be unique within your AWS account and region
- **GitHub repository name** must match exactly (case-sensitive) for OIDC to work
- **Email address** is required for CloudWatch alarm notifications
- After changing stack names, update the workflow file accordingly

---
